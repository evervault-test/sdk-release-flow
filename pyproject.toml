[tool.poetry]
name = "sdk-release-flow"
version = "2.2.0"
description = ""
authors = ["DeirdreCleary <deirdre@evervault.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
